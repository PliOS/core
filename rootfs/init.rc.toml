[environment]
LANG = "UTF-8"

[triggers]
init = [
    "mount ext4 /dev/root / remount", 
    "start sh",
]

shutdown = [
    "stop sh",
    "umount /",
    "reboot shutdown",
]

[services.sh]
program = "sh"
